# Telegram 群聊频道监控工具 — 架构优化方案

---

## 一、现状诊断

基于当前代码结构（`web_app.py` 约 52KB 单体文件）和部署形态，梳理出以下核心问题：

### 问题 1: 单体架构

- `web_app.py` 混在一起：Web 服务器 + Telegram 客户端 + 监控逻辑 + 推送逻辑
- 一个模块挂了，整个服务挂

### 问题 2: SQLite 单文件

- 多账号并发写入时锁竞争，量大后会卡
- 单点故障，无备份

### 问题 3: 进程管理薄弱

- systemd 单进程，Telegram 连接断了没有自动重连策略
- 账号掉线没有告警

### 问题 4: 可观测性差

- 日志只写文件，没有监控指标
- 出问题只能翻日志，不能提前预警

### 问题 5: 敏感信息暴露

- session 文件、api_hash、webhook key 全在明文 config.json

---

## 二、架构优化方案

### 现状 → 目标

```
┌─ 现状（单体）───────────────────────┐
│   web_app.py 一个进程                │
│   ├─ Web 管理后台                   │
│   ├─ Telegram 客户端 x N            │
│   ├─ 监控规则引擎                   │
│   ├─ 消息转发 / Webhook             │
│   └─ SQLite 读写                    │
└─────────────────────────────────────┘


┌─ 目标（分层解耦）───────────────────┐
│                                     │
│  ① Web 层（FastAPI）                │
│     ├─ 管理后台                     │
│     └─ 实时日志 WebSocket            │
│                                     │
│  ② 监控引擎层（独立进程）            │
│     ├─ 每账号独立 worker             │
│     ├─ 断线自动重连                  │
│     └─ 规则匹配引擎                  │
│                                     │
│  ③ 消息队列（Redis/NATS）           │
│     └─ Web 层 ↔ 引擎层 解耦          │
│                                     │
│  ④ 存储层                           │
│     ├─ PostgreSQL（历史消息）        │
│     └─ Redis（状态/缓存）            │
│                                     │
│  ⑤ 推送层（独立 worker）            │
│     ├─ 企业微信 Webhook              │
│     ├─ Telegram Bot                  │
│     └─ 失败重试 + 降级               │
│                                     │
└─────────────────────────────────────┘
```

---

## 三、分模块优化清单

### 1. 监控引擎层

| 优化项 | 现状 | 方案 |
|--------|------|------|
| 账号隔离 | 单进程跑所有账号 | 每账号独立 worker，一个号出问题不影响其他号 |
| 断线重连 | 无 | 指数退避自动重连，连续失败告警 |
| 会话管理 | session 文件明文 | 加密存储，定期热更新 |
| 连接池 | 每次新建 | Telethon 客户端复用，长连接保活 |

### 2. 存储层

| 优化项 | 现状 | 方案 |
|--------|------|------|
| 数据库 | SQLite 单文件 | PostgreSQL（JSONB 存消息，全文索引查关键词） |
| 并发写入 | 锁竞争 | 连接池 + 批量写入 |
| 备份 | 无 | 每日自动备份 + 异地存储 |
| 消息去重 | 未知 | 消息 ID + 账号 ID 唯一索引，防重复推送 |

### 3. 推送层

| 优化项 | 现状 | 方案 |
|--------|------|------|
| 失败重试 | 无 | 指数退避重试（3 次），失败降级到备用通道 |
| 限流 | 无 | 每通道独立限流，防刷屏 |
| 队列 | 同步推送 | 异步队列，推送不阻塞监控 |
| 多通道 | 企业微信 + Telegram Bot | 扩展钉钉/飞书/邮件，统一抽象 |

### 4. 安全层

| 优化项 | 现状 | 方案 |
|--------|------|------|
| session/密钥 | 明文 config.json | 环境变量或 Vault 加密存储 |
| 管理后台 | 单管理员 | 多用户 + 角色权限 + 2FA |
| 登录防护 | 无 | 登录限流 + 失败锁定 |
| API | 无鉴权 | JWT token + 访问审计日志 |

### 5. 可观测性

| 优化项 | 现状 | 方案 |
|--------|------|------|
| 监控指标 | 无 | Prometheus + Grafana 面板 |
| 告警 | 无 | 账号掉线/推送失败/消息积压自动告警 |
| 日志 | 写文件 | 结构化日志 + 集中收集（Loki） |
| 健康检查 | 无 | /healthz 端点 + 心跳检测 |

---

## 四、功能增强方向

### 1. 消息智能过滤
- 关键词 → 正则 → 语义理解（可选接入 LLM）
- 按发送者优先级排序

### 2. 监控统计
- 每日消息量、命中率、各账号活跃度
- Web 后台可视化图表

### 3. 多租户
- 一套系统服务多个用户
- 每个用户独立账号/规则/通知

### 4. 历史回溯
- 消息全文搜索（模糊匹配）
- 按时间线导出报表

### 5. 移动端适配
- 响应式前端
- PWA 推送通知

---

## 五、分阶段路线图

### 第一阶段 稳（1 周）· 零风险

| 任务 | 说明 |
|------|------|
| 断线自动重连 | 账号断开后指数退避重连，持续失败告警 |
| 消息去重 | 消息 ID + 账号 ID 唯一索引，防止重复推送 |
| 推送失败重试 | 推送失败自动重试 3 次，降级备用通道 |
| 每日数据库备份 | 自动备份 SQLite/PostgreSQL，保留最近 7 天 |
| 健康检查 + 账号掉线告警 | 提供 /healthz 端点，掉线时企业微信通知 |

> 目标：跑一个月不死、不漏、不重

### 第二阶段 分（2 周）· 架构拆分

| 任务 | 说明 |
|------|------|
| Web 层 / 引擎层 / 推送层 拆开 | 独立进程，独立部署 |
| 每账号独立 worker | 一个号出问题不影响其他号 |
| 引入消息队列（Redis） | Web 层与引擎层解耦，异步通信 |

> 目标：单点故障不影响全局

### 第三阶段 换（1 周）· 存储升级

| 任务 | 说明 |
|------|------|
| SQLite → PostgreSQL | 高性能，支持并发 |
| 全文搜索索引 | 关键词搜索毫秒级响应 |
| 消息去重唯一约束 | 数据库层面保证不重复 |

> 目标：百万级消息不卡

### 第四阶段 看（1 周）· 可观测性

| 任务 | 说明 |
|------|------|
| Prometheus 指标 | 消息量、命中率、推送成功率等指标暴露 |
| Grafana 面板 | 可视化仪表盘，实时监控运行状态 |
| 集中日志 + 告警 | Loki 收集日志，告警规则自动通知 |

> 目标：出问题 5 分钟内定位

### 第五阶段 强（2 周）· 功能增强

| 任务 | 说明 |
|------|------|
| 多用户/多租户 | 支持多用户独立使用 |
| 智能过滤（可选 LLM） | 语义理解过滤无关消息 |
| 统计分析面板 | 每日消息量、命中率、活跃度图表 |
| 移动端适配 | 响应式布局，PWA 推送 |

> 目标：可商业化，给投资方看

---

## 六、资源评估

| 阶段 | 资源需求 | 建议配置 |
|------|---------|---------|
| 1-2 阶段 | 现有够用 | 2 核 3G |
| 3 阶段起 | PostgreSQL + Redis | 4 核 8G |
| 5 阶段 | 多租户 + 分析 | 8 核 16G 起步 |