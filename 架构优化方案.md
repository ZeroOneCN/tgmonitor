# Telegram 群聊频道监控工具 — 架构优化方案

> 版本：v2.0 | 更新日期：2026-08-18
> 核心原则：保留单体架构，保留 SQLite，在现有基础上增量演进

---

## 一、项目现状

### 1.1 当前架构

```
┌─ 现状（单体）──────────────────────┐
│   web_app.py 单进程（~52KB）         │
│   ├─ FastAPI Web 管理后台            │
│   ├─ Telegram 客户端 x N            │
│   ├─ 监控规则引擎                    │
│   ├─ 消息转发 / Webhook 推送         │
│   ├─ 企业微信媒体上传                │
│   ├─ 用户认证 / Session 管理         │
│   ├─ SQLite 历史消息存储            │
│   └─ 实时日志 WebSocket              │
└─────────────────────────────────────┘
```

### 1.2 已完成功能

| 模块 | 功能 | 状态 |
|------|------|------|
| Web 管理后台 | 账号管理、规则配置、实时日志、历史消息 | ✅ 已完成 |
| 用户认证 | 账号密码登录、Session 管理、首次初始化 | ✅ 已完成 |
| 多账号监控 | 每账号独立 session、代理配置、规则匹配 | ✅ 已完成 |
| 消息转发 | 转发到 Saved Messages | ✅ 已完成 |
| 企业微信推送 | 文本 + 图片（base64）+ 视频/文件（media_id） | ✅ 已完成 |
| 贴纸格式转换 | webp/gif 转为标准 JPEG 后推送 | ✅ 已完成 |
| 媒体压缩 | 图片超 2MB 自动压缩 | ✅ 已完成 |
| 历史消息 | SQLite 存储、Web 界面筛选、分页 | ✅ 已完成 |
| UTC+8 时间 | 日志、通知、历史消息统一时区 | ✅ 已完成 |
| PM2 部署 | 进程管理、Nginx 反向代理配置 | ✅ 已完成 |
| 掉线告警 | 账号断线检测 + 自动重连 | ✅ 已完成 |

### 1.3 待解决问题

| 编号 | 问题 | 影响 | 优先级 |
|------|------|------|--------|
| Q1 | 无消息去重，重启/重连可能重复推送 | 用户体验 | P0 |
| Q2 | 推送失败无重试，直接丢弃 | 数据丢失 | P0 |
| Q3 | 单管理员，无法多用户使用 | 功能缺失 | P1 |
| Q4 | 历史消息只支持 LIKE 模糊匹配 | 性能 | P1 |
| Q5 | 无通知频率限制，高频消息会刷屏 | 用户体验 | P1 |
| Q6 | 无定时清理，历史数据无限增长 | 存储 | P2 |
| Q7 | 仅支持企业微信，无法扩展其他平台 | 功能缺失 | P2 |
| Q8 | 页面未铺满，空间利用率低 | UI/UX | P2 |

---

## 二、目标架构

> 保留单体 + SQLite，通过模块化内聚 + 功能扩展提升系统能力

```
┌─ 目标（增强型单体）──────────────────────────────────────────┐
│                                                              │
│  Web 层（FastAPI）                                           │
│  ├─ 多租户管理后台（铺满式布局）                              │
│  │   ├─ 管理员视图：所有用户概览、系统统计                     │
│  │   └─ 普通用户视图：独立账号/规则/通知管理                   │
│  ├─ 实时日志 WebSocket（按用户隔离）                          │
│  ├─ 历史消息查询（FTS5 全文搜索）                             │
│  ├─ 定时清理管理（开关 + 日期 + 可备份）                      │
│  ├─ 消息统计面板（图表可视化）                                │
│  └─ 规则模板管理                                              │
│                                                              │
│  监控引擎层（进程内）                                         │
│  ├─ 多租户隔离：每用户独立 Telegram 账号 + 规则集              │
│  ├─ 消息去重：msg.id + user_id 唯一索引                       │
│  ├─ 断线自动重连（指数退避）                                  │
│  └─ 规则匹配引擎（关键词/用户/群组）                          │
│                                                              │
│  推送层（进程内，异步队列）                                   │
│  ├─ 统一推送抽象接口 Notifier                                 │
│  │   ├─ 企业微信 Webhook（图片 base64 / 视频 media_id）       │
│  │   ├─ 飞书 Webhook                                          │
│  │   ├─ 钉钉 Webhook                                          │
│  │   └─ Telegram Bot                                          │
│  ├─ 失败重试（3 次指数退避）                                  │
│  └─ 频率限制（每通道独立，防刷屏）                             │
│                                                              │
│  存储层                                                       │
│  ├─ SQLite（多租户分表或共享表 + user_id）                    │
│  ├─ FTS5 全文索引（历史搜索）                                 │
│  ├─ 定时备份（每日 + 保留 N 天）                              │
│  └─ 定时清理（可开关 + 日期选择 + 清理前自动备份）            │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 2.1 架构决策说明

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 进程模型 | 单进程 | 当前规模够用，避免运维复杂度 |
| 数据库 | SQLite | 单文件部署简单，WAL 模式并发足够 |
| 消息队列 | 进程内 asyncio.Queue | 不引入 Redis，降低依赖 |
| 可观测性 | 内置日志 + 掉线告警 | 已有掉线告警，无需额外组件 |
| 敏感信息 | 保持 config.json | 直接访问服务器也获取不到，物理安全已足够 |
| 备份 | cron 定时 cp | SQLite 单文件天然适合整文件备份 |

---

## 三、多租户设计

### 3.1 数据模型

```
┌─ users 表 ──────────────────────────┐
│ id              INTEGER PRIMARY KEY │
│ username        TEXT UNIQUE         │
│ password_hash   TEXT                │
│ password_salt   TEXT                │
│ role            TEXT DEFAULT 'user' │ -- admin / user
│ is_active       INTEGER DEFAULT 1   │
│ created_at      TEXT                 │
└─────────────────────────────────────┘

┌─ tg_accounts 表（原 accounts 扩展） ───────────┐
│ id              INTEGER PRIMARY KEY             │
│ user_id         INTEGER REFERENCES users(id)    │
│ remark          TEXT                             │
│ phone           TEXT                             │
│ api_id          INTEGER                          │
│ api_hash        TEXT                             │
│ proxy           TEXT (JSON)                      │
│ status          TEXT                             │ -- logged_out / active / disconnected
│ created_at      TEXT                             │
└────────────────────────────────────────────────┘

┌─ monitor_rules 表（原 rules 独立表）──────────────────────┐
│ id                INTEGER PRIMARY KEY                     │
│ user_id           INTEGER REFERENCES users(id)            │
│ account_id        INTEGER REFERENCES tg_accounts(id)      │
│ remark            TEXT                                     │
│ target_user_ids   TEXT (JSON)                             │
│ target_usernames  TEXT (JSON)                             │
│ chat_ids          TEXT (JSON)                             │
│ chat_titles       TEXT (JSON)                             │
│ keywords_include  TEXT (JSON)                             │
│ keywords_exclude  TEXT (JSON)                             │
│ forward_to_saved  INTEGER DEFAULT 1                       │
│ forward_to_chats  TEXT (JSON)                             │
│ enabled           INTEGER DEFAULT 1                       │
└──────────────────────────────────────────────────────────┘

┌─ notify_channels 表（多通知平台）────────────────────────────┐
│ id              INTEGER PRIMARY KEY                          │
│ user_id         INTEGER REFERENCES users(id)                │
│ platform        TEXT  -- wecom / feishu / dingtalk / tg_bot │
│ webhook_url     TEXT                                         │
│ bot_token       TEXT  -- Telegram Bot 专用                   │
│ chat_id         TEXT  -- Telegram Bot 专用                   │
│ enabled         INTEGER DEFAULT 1                           │
│ rate_limit      INTEGER DEFAULT 20  -- 每分钟最大推送数     │
│ created_at      TEXT                                         │
└─────────────────────────────────────────────────────────────┘

┌─ message_history 表（扩展 user_id）──────────────────┐
│ id              INTEGER PRIMARY KEY                 │
│ user_id         INTEGER REFERENCES users(id)        │
│ account_id      INTEGER                             │
│ chat_id         TEXT                                 │
│ chat_title      TEXT                                 │
│ sender_id       TEXT                                 │
│ sender_name     TEXT                                 │
│ text            TEXT                                 │
│ media_type      TEXT                                 │
│ msg_id          INTEGER                              │
│ matched_rule    TEXT                                 │
│ pushed          INTEGER DEFAULT 0                    │
│ created_at      TEXT                                 │
│ UNIQUE(msg_id, account_id)  -- 消息去重              │
└────────────────────────────────────────────────────┘
```

### 3.2 权限模型

```
管理员 (admin)
├─ 查看所有用户的账号状态、监控规则、通知配置
├─ 查看全局消息统计、系统健康状态
├─ 创建/停用/删除用户
├─ 管理规则模板
└─ 配置系统级定时任务（备份、清理）

普通用户 (user)
├─ 管理自己的 Telegram 账号
├─ 管理自己的监控规则
├─ 管理自己的通知渠道
├─ 查看自己的历史消息
└─ 查看自己的消息统计
```

### 3.3 管理员后台视图

```
┌─ 管理后台 ──────────────────────────────────────────────────┐
│                                                             │
│  用户管理                                                    │
│  ┌──────────┬──────────┬──────────┬────────┬──────────┐     │
│  │ 用户名    │ 角色      │ 账号数    │ 规则数  │ 状态      │     │
│  ├──────────┼──────────┼──────────┼────────┼──────────┤     │
│  │ alice    │ user      │ 2        │ 5      │ 活跃      │     │
│  │ bob      │ user      │ 1        │ 3      │ 活跃      │     │
│  │ admin    │ admin     │ -        │ -      │ 活跃      │     │
│  └──────────┴──────────┴──────────┴────────┴──────────┘     │
│                                                             │
│  全局统计                                                    │
│  ┌─ 今日消息量 ──┐ ┌─ 推送成功率 ──┐ ┌─ 活跃账号 ────┐       │
│  │   1,234       │ │    98.5%      │ │    8 / 10     │       │
│  └───────────────┘ └───────────────┘ └───────────────┘       │
│                                                             │
│  账号概览                                                    │
│  ┌──────────┬──────────┬──────────┬──────────┬──────────┐   │
│  │ 用户      │ 账号      │ 状态      │ 监控群组   │ 最后活动   │   │
│  ├──────────┼──────────┼──────────┼──────────┼──────────┤   │
│  │ alice    │ +86xxx   │ 在线      │ 3 个群     │ 2 分钟前   │   │
│  │ bob      │ +86yyy   │ 离线      │ 5 个群     │ 1 小时前   │   │
│  └──────────┴──────────┴──────────┴──────────┴──────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 四、功能增强清单

### 4.1 P0 — 核心稳定性

| 编号 | 功能 | 说明 | 实现方式 |
|------|------|------|---------|
| F1 | 消息去重 | 防止重启/重连后重复推送 | `message_history` 表 `UNIQUE(msg_id, account_id)` + INSERT OR IGNORE |
| F2 | 推送失败重试 | Webhook 推送失败后自动重试 | 3 次指数退避（1s → 2s → 4s），失败记入日志 |
| F3 | 通知频率限制 | 防止高频消息刷屏 | 每通知渠道独立计数器，超过 rate_limit 条/分钟则暂停推送并告警 |

### 4.2 P1 — 多租户与功能扩展

| 编号 | 功能 | 说明 | 实现方式 |
|------|------|------|---------|
| F4 | 多用户系统 | 每用户独立账号/规则/通知 | users 表 + user_id 关联，管理员可查看所有用户 |
| F5 | 多通知平台 | 支持飞书、钉钉等渠道 | 统一 `Notifier` 抽象接口，各平台实现 |
| F6 | SQLite FTS5 全文搜索 | 历史消息毫秒级搜索 | 为 `message_history.text` 建虚拟表 |
| F7 | 定时清理历史记录 | 可开关 + 日期选择 + 清理前备份 | APScheduler 定时任务，清理 N 天前数据 |
| F8 | 规则模板 | 常用监控规则保存为模板，一键套用 | `rule_templates` 表 + 模板管理页面 |

### 4.3 P2 — 体验优化

| 编号 | 功能 | 说明 | 实现方式 |
|------|------|------|---------|
| F9 | 页面铺满设计 | 充分利用屏幕宽度，信息密度更高 | CSS Grid 布局，移除固定宽度限制 |
| F10 | 消息统计面板 | 每日消息量、推送成功率图表 | 前端 Chart.js + 后端聚合查询 API |
| F11 | 关键词高亮 | 历史消息中匹配的关键词高亮 | 前端 JS 正则替换，`<mark>` 标签 |
| F12 | 批量操作 | 批量导出历史消息、批量管理规则 | 前端复选框 + 后端批量 API |

### 4.4 我补充的扩展建议

| 编号 | 功能 | 说明 | 理由 |
|------|------|------|------|
| F13 | 推送日志表 | 记录每次推送的结果（成功/失败/重试次数） | 方便排查"为什么没收到通知" |
| F14 | 账号健康检测 | 定期检测账号连接状态，异常自动告警 | 防止账号静默掉线 |
| F15 | 消息标签/收藏 | 对重要消息打标签或收藏 | 便于后续回溯关键信息 |
| F16 | Webhook URL 加密存储 | config.json 中的 webhook URL 简单加密 | 防止服务器被入侵后直接获取所有通知渠道 |
| F17 | 话题组（Topics）支持 | 区分同一群聊下不同讨论组，支持按话题过滤和推送 | 话题组在大型群聊中非常普遍，不区分会导致消息混乱 |

---

## 五、话题组（Topics）支持

### 5.1 背景说明

Telegram 的话题组（Forum Topics）允许在同一个超级群组中创建多个讨论分支。每一条消息都属于某个话题，话题通过 `topic_id` 标识。

```
┌─ 群聊：技术交流群 ──────────────────────────────────────┐
│                                                          │
│  # 公告            ← topic_id=1                          │
│  ├─ 系统维护通知                                          │
│  ├─ 版本更新说明                                          │
│                                                          │
│  # 技术讨论         ← topic_id=5                          │
│  ├─ 有人用过FastAPI吗？                                    │
│  ├─ 推荐这个ORM库                                         │
│                                                          │
│  # 灌水区           ← topic_id=12                         │
│  ├─ 今天天气不错                                          │
│  └─ 中午吃什么                                             │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

### 5.2 数据模型扩展

#### 消息存储增加话题字段

```sql
-- message_history 表扩展
ALTER TABLE message_history ADD COLUMN topic_id INTEGER DEFAULT 0;
ALTER TABLE message_history ADD COLUMN topic_name TEXT DEFAULT '';
```

#### 监控规则增加话题过滤

```json
{
  "remark": "监控技术讨论",
  "chat_ids": [-1001234567890],
  "topic_ids": [5],              // 只监控 topic_id=5 的话题
  "topic_names": [],             // 或按名称过滤（可选，不填则监控所有话题）
  "topic_match_mode": "include", // include = 仅监控指定话题, exclude = 排除指定话题, all = 不区分话题
  ...
}
```

### 5.3 Telethon 中的话题识别

```python
# 1. 判断消息是否属于话题组
if message.reply_to and getattr(message.reply_to, 'reply_to_top_id', None):
    topic_id = message.reply_to.reply_to_top_id
else:
    topic_id = 0  # 主讨论区（General）

# 2. 获取话题名称（通过话题ID查询缓存或API）
async def get_topic_name(client, chat_id, topic_id):
    """获取话题名称"""
    if topic_id == 0:
        return "主讨论区"
    from telethon.tl.functions.channels import GetForumTopicsRequest
    result = await client(GetForumTopicsRequest(
        channel=chat_id,
        offset_date=0,
        offset_id=0,
        offset_topic=0,
        limit=50
    ))
    for topic in result.topics:
        if topic.id == topic_id:
            return topic.title
    return f"话题#{topic_id}"

# 3. 话题名称缓存（避免频繁API调用）
topic_name_cache = {}  # key: f"{chat_id}:{topic_id}", value: (name, expire_time)
```

### 5.4 消息匹配逻辑

```
收到消息
  ├─ 获取消息的 topic_id 和 topic_name
  ├─ 遍历规则列表
  │   ├─ 规则有 topic_ids 限制？
  │   │   ├─ 是 → topic_id 是否在 topic_ids 中？
  │   │   │   ├─ 在 → 继续匹配其他条件
  │   │   │   └─ 不在 → 跳过此规则
  │   │   └─ 否 → 继续匹配其他条件
  │   ├─ 规则有 topic_names 限制？
  │   │   ├─ 是 → topic_name 是否匹配？
  │   │   │   ├─ 匹配 → 继续匹配其他条件
  │   │   │   └─ 不匹配 → 跳过此规则
  │   │   └─ 否 → 继续匹配其他条件
  │   ├─ topic_match_mode = exclude？
  │   │   ├─ 是 → topic_id 在排除列表中 → 跳过此规则
  │   │   └─ 否 → 继续匹配其他条件
  │   └─ 继续匹配关键字/用户/群组等条件
  └─ 匹配成功 → 推送/转发
```

### 5.5 Webhook 通知中的话题展示

```markdown
**消息来源**: 技术交流群 → **技术讨论**
**发送者**: 张三
**内容**: 有人用过FastAPI吗？
```

如果消息来自主讨论区（General），则不显示话题信息：

```markdown
**消息来源**: 技术交流群
**发送者**: 李四
**内容**: 大家好
```

### 5.6 前端配置界面

```
┌─ 规则编辑 ─────────────────────────────────────────────────────┐
│                                                                │
│  规则名称: [监控技术讨论                          ]             │
│                                                                │
│  监控群组: [技术交流群 (-1001234567890)           ] [添加]      │
│                                                                │
│  ☑ 话题过滤                                                     │
│     过滤模式: [● 仅监控指定话题  ○ 排除指定话题  ○ 不区分话题]   │
│                                                                │
│     选择话题:                                                   │
│     ☑ # 公告 (topic_id=1)                                      │
│     ☑ # 技术讨论 (topic_id=5)                                   │
│     ☐ # 灌水区 (topic_id=12)                                    │
│     [刷新话题列表]  ← 点击后从Telegram拉取最新话题列表           │
│                                                                │
│  关键词包含: [FastAPI, Python                    ]              │
│  关键词排除: []                                                 │
│                                                                │
│  ...其他规则配置...                                             │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### 5.7 话题缓存策略

由于 `GetForumTopicsRequest` 有 API 调用频率限制，需要缓存：

```python
class TopicCache:
    """话题名称缓存，避免频繁调用 Telegram API"""
    def __init__(self):
        self._cache = {}  # key: f"{chat_id}", value: {topics: [...], expires_at: timestamp}

    async def get_topics(self, client, chat_id: int) -> list:
        """获取话题列表（带缓存）"""
        key = str(chat_id)
        now = time.time()
        if key in self._cache and self._cache[key]["expires_at"] > now:
            return self._cache[key]["topics"]

        from telethon.tl.functions.channels import GetForumTopicsRequest
        result = await client(GetForumTopicsRequest(
            channel=chat_id,
            offset_date=0,
            offset_id=0,
            offset_topic=0,
            limit=50
        ))
        topics = [{"id": t.id, "title": t.title} for t in result.topics]
        self._cache[key] = {"topics": topics, "expires_at": now + 3600}  # 缓存1小时
        return topics

    def get_topic_name(self, chat_id: int, topic_id: int) -> str:
        """从缓存中获取话题名称"""
        topics = self._cache.get(str(chat_id), {}).get("topics", [])
        for t in topics:
            if t["id"] == topic_id:
                return t["title"]
        return f"话题#{topic_id}"
```

### 5.8 阶段性实施

| 阶段 | 任务 | 说明 |
|------|------|------|
| 阶段一 | 基础话题识别 | 消息存储增加 topic_id/topic_name，日志和通知中展示话题来源 |
| 阶段二 | 规则话题过滤 | 规则支持 topic_ids/topic_names 匹配，前端配置界面 |
| 阶段三 | 话题缓存与优化 | 完善缓存策略，支持话题列表刷新，异常处理 |

---

## 六、通知平台抽象设计

### 6.1 统一接口

```python
class Notifier:
    """通知平台统一接口"""
    platform: str  # wecom / feishu / dingtalk / tg_bot

    async def send_text(self, webhook_url: str, text: str) -> tuple[bool, str]:
        """发送文本消息，返回 (是否成功, 错误信息)"""
        ...

    async def send_image(self, webhook_url: str, file_bytes: bytes, filename: str) -> tuple[bool, str]:
        """发送图片，返回 (是否成功, 错误信息)"""
        ...

    async def send_file(self, webhook_url: str, file_bytes: bytes, filename: str) -> tuple[bool, str]:
        """发送文件/视频，返回 (是否成功, 错误信息)"""
        ...
```

### 6.2 各平台实现差异

| 平台 | 图片发送方式 | 视频/文件发送方式 | 文本格式 |
|------|-------------|-------------------|---------|
| 企业微信 | base64 + md5 直发 | upload_media → media_id | markdown |
| 飞书 | upload_image → image_key | upload_file → file_key | 富文本 (post) |
| 钉钉 | upload_media → media_id | upload_media → media_id | markdown |
| Telegram Bot | sendPhoto (multipart) | sendDocument / sendVideo | HTML |

### 6.3 飞书 Webhook 参考

```
POST https://open.feishu.cn/open-apis/bot/v2/hook/{token}

文本消息:
{
  "msg_type": "text",
  "content": { "text": "消息内容" }
}

富文本消息（支持图片）:
{
  "msg_type": "post",
  "content": {
    "post": {
      "zh_cn": {
        "title": "Telegram监控告警",
        "content": [[{"tag": "text", "text": "内容"}, {"tag": "img", "image_key": "..."}]]
      }
    }
  }
}

上传图片:
POST https://open.feishu.cn/open-apis/im/v1/images
Form: image_type=message, image=<binary>
返回: {"code": 0, "data": {"image_key": "..."}}
```

### 6.4 钉钉 Webhook 参考

```
POST https://oapi.dingtalk.com/robot/send?access_token={token}

文本消息:
{
  "msgtype": "text",
  "text": { "content": "消息内容" }
}

Markdown 消息:
{
  "msgtype": "markdown",
  "markdown": { "title": "告警", "text": "**内容**" }
}

上传媒体:
POST https://oapi.dingtalk.com/robot/upload_media?access_token={token}&type=image
Form: media=<binary>
返回: {"media_id": "...", "errcode": 0}
```

---

## 七、定时清理与备份设计

### 7.1 定时清理

```
┌─ 清理配置 ──────────────────────────────┐
│                                         │
│  ☑ 启用定时清理                          │
│                                         │
│  保留天数: [30] 天                       │
│                                         │
│  执行时间: [03:00]  (每日)                │
│                                         │
│  ☑ 清理前自动备份                        │
│    备份保留: [7] 天                      │
│                                         │
│  清理范围:                               │
│  ☑ 消息历史 (message_history)            │
│  ☐ 推送日志 (push_logs)                 │
│  ☐ 消息标签 (message_tags)              │
│                                         │
│           [保存配置]                      │
└─────────────────────────────────────────┘
```

### 7.2 自动备份

| 项目 | 方案 |
|------|------|
| 触发方式 | APScheduler cron 定时任务 |
| 执行时间 | 每日凌晨 03:00 |
| 备份方式 | `cp history.db history.db.bak.{date}` |
| 保留策略 | 保留最近 7 天，超出自动删除 |
| 清理前备份 | 定时清理执行前先触发一次备份 |
| 备份目录 | `./backups/` |

### 7.3 备份文件管理

```
backups/
├── history.db.bak.20260818
├── history.db.bak.20260817
├── history.db.bak.20260816
├── history.db.bak.20260815
├── history.db.bak.20260814
├── history.db.bak.20260813
└── history.db.bak.20260812  ← 最旧的，明天会被删
```

---

## 八、页面铺满设计

### 8.1 布局方案

```
┌─────────────────────────────────────────────────────────────────┐
│  [Logo] Telegram 群聊频道监控后台              [用户] [退出]      │ ← 顶栏 (60px)
├─────────────────────────────────────────────────────────────────┤
│ ┌─侧栏─┐ ┌─主内容区（铺满剩余宽度）──────────────────────────┐ │
│ │      │ │                                                       │ │
│ │ 概览 │ │  ┌─账号状态────────┐ ┌─今日统计────────┐            │ │
│ │ 账号 │ │  │ ● alice 在线    │ │ 消息: 1,234     │            │ │
│ │ 规则 │ │  │ ● bob 离线      │ │ 推送: 98.5%     │            │ │
│ │ 通知 │ │  └─────────────────┘ └─────────────────┘            │ │
│ │ 历史 │ │                                                       │ │
│ │ 统计 │ │  ┌─实时日志（WebSocket）────────────────────────┐    │ │
│ │ 设置 │ │  │ [20:03] Webhook [0] 图片推送成功              │    │ │
│ │      │ │  │ [20:02] 消息匹配规则: 关键词"xxx"             │    │ │
│ │      │ │  │ [20:01] 账号 alice 已连接                     │    │ │
│ │      │ │  └───────────────────────────────────────────────┘    │ │
│ │      │ │                                                       │ │
│ └──────┘ └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

### 8.2 CSS 布局核心

```css
/* 整体铺满 */
body { margin: 0; height: 100vh; overflow: hidden; }
.app-layout { display: grid; grid-template-columns: 220px 1fr; height: 100vh; }
.sidebar { background: #1a1a2e; overflow-y: auto; }
.main { padding: 20px; overflow-y: auto; }

/* 卡片网格（自动铺满） */
.card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
  gap: 16px;
}

/* 统计卡片 */
.stat-card { padding: 20px; border-radius: 12px; }

/* 表格铺满 */
.table-container { width: 100%; overflow-x: auto; }
table { width: 100%; border-collapse: collapse; }
```

### 8.3 响应式断点

| 屏幕宽度 | 布局 | 侧栏 | 卡片列数 |
|---------|------|------|---------|
| ≥ 1920px | 铺满 | 展开 (240px) | 4 列 |
| 1440-1919px | 铺满 | 展开 (220px) | 3 列 |
| 1024-1439px | 铺满 | 展开 (200px) | 2 列 |
| 768-1023px | 铺满 | 折叠（图标） | 1 列 |
| < 768px | 移动端 | 抽屉式 | 1 列 |

---

## 九、消息统计面板

### 9.1 统计指标

| 指标 | 数据来源 | 展示方式 |
|------|---------|---------|
| 每日消息量 | `message_history` 按天 GROUP BY | 折线图 |
| 推送成功率 | `push_logs` 成功/总数 | 环形图 |
| 各账号消息量 | `message_history` 按 account_id GROUP BY | 柱状图 |
| 规则命中率 | `message_history.matched_rule` GROUP BY | 排行榜 |
| 活跃群组 TOP10 | `message_history` 按 chat_title GROUP BY | 水平柱状图 |
| 推送渠道分布 | `push_logs` 按 platform GROUP BY | 饼图 |

### 9.2 API 设计

```
GET /api/stats/daily?days=7          → 近 7 天每日消息量
GET /api/stats/push-success?days=7   → 近 7 天推送成功率
GET /api/stats/account-summary        → 各账号消息概览
GET /api/stats/rule-hit-rate          → 规则命中率排行
GET /api/stats/top-chats?limit=10     → 活跃群组 TOP10
GET /api/stats/platform-distribution  → 推送渠道分布
```

### 9.3 前端图表

使用 Chart.js（轻量，无需框架）：

```javascript
// 每日消息量折线图
new Chart(ctx, {
  type: 'line',
  data: {
    labels: dates,
    datasets: [{
      label: '消息量',
      data: counts,
      borderColor: '#00a8ff',
      tension: 0.3
    }]
  }
});
```

---

## 十、数据库迁移

### 10.1 迁移策略

从现有单管理员 `config.json` 迁移到多租户 SQLite：

```
步骤 1: 创建新表（users, tg_accounts, monitor_rules, notify_channels）
步骤 2: 迁移现有 config.json 数据
        - 创建默认 admin 用户
        - 将现有 accounts 迁移到 tg_accounts（user_id = 1）
        - 将现有 rules 迁移到 monitor_rules
        - 将现有 webhooks 迁移到 notify_channels（platform = 'wecom'）
步骤 3: message_history 表增加 user_id 列，默认 1
步骤 4: 创建 FTS5 虚拟表索引
步骤 5: config.json 仅保留系统级配置（端口、时区等）
```

### 10.2 迁移脚本

```python
def migrate_v3_to_v4(db_path):
    """从 v3 (config.json 单管理员) 迁移到 v4 (多租户 SQLite)"""
    # 1. 创建新表
    # 2. 创建默认 admin 用户
    # 3. 读取 config.json，迁移 accounts/rules/webhooks
    # 4. 更新 message_history 增加 user_id
    # 5. 创建 FTS5 索引
    # 6. 更新 config.json version = 4
```

---

## 十一、分阶段实施计划

### 阶段一 · 稳固（P0 核心）

> 目标：不漏、不重、不丢

| 编号 | 任务 | 说明 |
|------|------|------|
| 1.1 | 消息去重 | `message_history` 加 `UNIQUE(msg_id, account_id)`，INSERT OR IGNORE |
| 1.2 | 推送失败重试 | 3 次指数退避（1s → 2s → 4s），失败记入 `push_logs` 表 |
| 1.3 | 通知频率限制 | 每通知渠道独立计数器，超过 rate_limit 条/分钟暂停 |
| 1.4 | 推送日志表 | 新建 `push_logs` 表，记录每次推送结果 |
| 1.5 | 自动备份 | APScheduler 每日 03:00 备份 SQLite，保留 7 天 |

### 阶段二 · 多租户（P1 功能）

> 目标：多用户独立使用

| 编号 | 任务 | 说明 |
|------|------|------|
| 2.1 | 数据库迁移 | 创建多租户表结构，迁移现有数据 |
| 2.2 | 多用户认证 | users 表 + 角色 + Session 隔离 |
| 2.3 | 管理员后台 | 全局用户列表、账号概览、系统统计 |
| 2.4 | 用户隔离 | 每用户只能看到自己的账号/规则/通知/历史 |
| 2.5 | 定时清理 | APScheduler + 清理前备份 + 开关配置 |

### 阶段三 · 扩展（P2 功能）

> 目标：多平台通知 + 全文搜索 + 话题组支持

| 编号 | 任务 | 说明 |
|------|------|------|
| 3.1 | Notifier 抽象 | 统一接口，各平台实现 |
| 3.2 | 飞书 Webhook | 文本 + 图片上传 + 富文本 |
| 3.3 | 钉钉 Webhook | 文本 + 媒体上传 + markdown |
| 3.4 | FTS5 全文搜索 | 历史消息毫秒级搜索 |
| 3.5 | 规则模板 | 常用规则保存为模板，一键套用 |
| 3.6 | 话题组基础识别 | 消息存储增加 topic_id/topic_name，通知中展示话题来源 |
| 3.7 | 话题组规则过滤 | 规则支持 topic_ids/topic_names 匹配，前端配置界面 |

### 阶段四 · 体验（P2 UI/UX）

> 目标：页面铺满 + 数据可视化

| 编号 | 任务 | 说明 |
|------|------|------|
| 4.1 | 页面铺满重设计 | CSS Grid 布局，移除固定宽度 |
| 4.2 | 消息统计面板 | Chart.js 图表，6 项指标 |
| 4.3 | 关键词高亮 | 历史消息搜索结果高亮匹配词 |
| 4.4 | 批量操作 | 批量导出、批量管理规则 |

---

## 十二、文件结构

```
007Telegram群消息监控工具/
├── web_app.py              # 主程序（FastAPI + 监控引擎）
├── config.json             # 系统级配置（端口、时区等）
├── history.db              # SQLite 数据库（多租户数据）
├── requirements.txt        # Python 依赖
├── ecosystem.config.js     # PM2 配置
├── tg-monitor.nginx.conf   # Nginx 配置
├── Telegram_logo.svg       # Logo
├── backups/                # 自动备份目录
│   ├── history.db.bak.20260818
│   └── ...
├── session_{phone}.session # Telegram session 文件（每账号一个）
├── templates/
│   ├── login.html          # 登录页
│   ├── setup.html          # 首次初始化页
│   ├── index.html          # 用户主页（铺满布局）
│   └── admin.html          # 管理员后台
└── static/
    ├── chart.min.js         # Chart.js
    └── style.css            # 铺满布局样式
```

---

## 十三、新增依赖

| 包名 | 用途 | 是否必须 |
|------|------|---------|
| `apscheduler>=3.10` | 定时任务（备份、清理） | 必须 |
| `cryptography>=42.0` | Webhook URL 加密存储 | 可选 |

> Pillow 已在现有 requirements.txt 中，无需新增

---

## 十四、验收清单

### 阶段一验收

- [ ] 重启服务后不会重复推送已有消息
- [ ] Webhook 推送失败后自动重试 3 次
- [ ] 高频消息不会刷屏（频率限制生效）
- [ ] push_logs 表有完整推送记录
- [ ] 每日 03:00 自动生成备份文件
- [ ] 备份文件 7 天后自动删除

### 阶段二验收

- [ ] 多用户可以独立注册/登录
- [ ] 管理员可查看所有用户及其账号状态
- [ ] 用户 A 看不到用户 B 的数据
- [ ] 定时清理可配置开关和保留天数
- [ ] 清理前自动生成备份

### 阶段三验收

- [ ] 飞书 Webhook 可推送文本和图片
- [ ] 钉钉 Webhook 可推送文本和图片
- [ ] 历史消息搜索响应 < 100ms
- [ ] 规则模板可保存和一键应用
- [ ] 话题消息自动识别 topic_id 和 topic_name
- [ ] Webhook 通知中展示话题来源（如 "技术交流群 → 技术讨论"）
- [ ] 规则可按话题 ID 或名称过滤消息
- [ ] 前端规则编辑页支持话题选择和刷新

### 阶段四验收

- [ ] 页面在 1920px 屏幕铺满无空白
- [ ] 统计面板显示 6 项指标图表
- [ ] 历史消息搜索关键词高亮显示
- [ ] 可批量导出历史消息为 CSV
