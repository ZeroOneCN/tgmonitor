# Telegram 群聊频道监控工具 — 部署与通知增强方案

## 一、用户登录（安全防护）

### 问题澄清
- **这里的登录指的是「网站用户登录」**，不是退出监控的 Telegram 账号
- 即访问 Web 管理后台需要输入管理员用户名和密码才能进入
- 退出登录只是清除当前浏览器的网站会话，不影响正在运行的监控任务
- 监控任务即使网站退出登录，依然在后台继续运行

### 方案：账号密码登录 + Session 会话

| 项目 | 说明 |
|------|------|
| 认证方式 | 网站管理员 用户名 + 密码 |
| 会话管理 | 后端生成 session token，前端 Cookie 存储 |
| 密码存储 | bcrypt 哈希，config.json 不存明文 |
| 实现难度 | 低（FastAPI + 前端页面） |
| 依赖 | `passlib[bcrypt]` + `itsdangerous` |
| 适配需求 | 无固定 IP，公网访问需要登录，完全满足 |

### 流程
1. 首次启动时，config.json 中若无密码，要求在 Web 界面设置管理员密码
2. 访问任意页面，未登录自动跳转到 `/login` 登录页
3. 输入正确用户名密码后，生成 7 天有效期的 session
4. 点击「退出登录」清除 session，需要重新登录才能访问

### 安全增强
- HTTPS 反向代理（Nginx + Let's Encrypt，必须）
- 登录失败次数限制（连续 5 次错误锁定 15 分钟，防暴力破解）

---

## 二、通知方案对比

### 需求说明
- 目的：转发到国内方便查看，挂 TG/VPN 不方便
- 需要：支持图片、视频、贴纸、文件**全部转发**
- Telegram Bot 因为需要 VPN 看，所以不用

### 可选方案整理

| 方案 | 平台 | 图片支持 | 免费额度 | 难度 | 推荐度 |
|------|------|---------|---------|------|--------|
| **企业微信机器人** | 微信 | ✅ 支持图片上传 | 免费（无限制） | 低 | ⭐⭐⭐⭐⭐ **首选** |
| **微信公众号模板消息** | 微信公众号 | 需URL，需认证 | 每月 1000 条免费 | 高 | ⭐⭐ |
| **PushDeer** | iOS/Android App | ✅ 支持图片（原生推送） | 免费开源自托管 | 低 | ⭐⭐⭐⭐ |
| **Bark** | iOS | ❌ 仅文本 | 免费 | 低 | ⭐⭐ |
| **Server酱·Turbo** | 微信公众号 | ✅ 支持图片 | 免费额度有限 | 低 | ⭐⭐⭐ |
| **飞书机器人** | 飞书 | ✅ 支持图片上传 | 免费（无限制） | 低 | ⭐⭐⭐⭐ |
| **钉钉机器人** | 钉钉 | ✅ 支持图片上传 | 免费（无限制） | 低 | ⭐⭐⭐⭐ |
| **自定义 Webhook** | 自有服务 | ✅ 取决于实现 | — | 高 | ⭐⭐ |

### 推荐方案：企业微信机器人（完整支持媒体转发）

企业微信机器人的 API 原生支持上传图片、文件，完全可以满足需求：

**工作流程：**
```
消息到达 → 监控账号收到 → 判断 media 类型
                         ├─ 纯文本 → 直接发送 text 消息
                         ├─ 图片/文件 → 下载到内存 → 调用企业微信 API 上传 → 推送
                         ├─ 视频/贴纸 → 下载 → 上传推送
```

**优点：**
- 在微信/企业微信里查看，国内网络直接打开，不用 VPN
- **支持图片、视频、文件完整推送**，不是只有文本
- 完全免费，无条数限制
- 配置简单，只需要复制 Webhook URL 填入配置

**配置项：**
- `enabled`: 是否启用（true/false）
- `webhook_url`: 企业微信机器人 Webhook URL
- `display_text`: 是否在通知中附带文字内容
- `send_media`: 是否发送媒体文件（图片、视频、文件）

---

## 三、图片/媒体消息处理

### 需求确认
- **图片、视频、贴纸、文件全部需要转发**
- 所有媒体类型都支持通过企业微信机器人推送

### 实现方案

| 媒体类型 | 处理方式 |
|---------|---------|
| 文本 | 直接发送 text 消息 |
| 图片 | 下载 → 企业微信 API `upload_media` 上传 → 推送 image 消息 |
| 视频 | 下载 → 上传 → 推送 video 消息 |
| 文件/文档 | 下载 → 上传 → 推送 file 消息 |
| 贴纸 | 下载（通常是 WEBP/PNG）→ 作为图片推送 |
| 语音/音频 | 下载 → 上传 → 推送 voice/文件 |
| GIF 动图 | 下载 → 作为图片/文件推送 |

### 存储策略
- 下载到内存，直接上传企业微信，不存磁盘（省空间，避免磁盘写操作）
- 转发完成后自动释放内存，无残留文件
- 历史消息记录在数据库中，媒体信息保留 URL 链接

---

## 四、历史消息记录

### 需求：Web 界面显示并保存历史记录，按日期随时查看

### 实现方案

| 项目 | 设计 |
|------|------|
| 存储方式 | SQLite 数据库（`history.db`），轻量无需额外服务 |
| 表结构 | `id`, `timestamp`, `account_name`, `chat_title`, `chat_id`, `sender_name`, `sender_id`, `text`, `has_media`, `media_type`, `media_path`, `forwarded` |
| 分页 | 按时间倒序排列，每页 20 条 |
| 筛选 | 按账号筛选、按日期范围筛选、按关键词搜索 |
| Web 界面 | 新增「历史消息」标签页，独立于「账号管理」「设置」 |

**访问流程：**
1. 顶部导航点击「历史消息」标签
2. 默认显示最近 30 天所有消息
3. 可选择账号、输入关键词、选择起止日期筛选
4. 点击某条消息展开详情
5. 媒体消息显示缩略图/链接，可点击下载

---

## 五、时间统一为 UTC+8

### 需求确认
- 所有日志输出统一使用 `Asia/Shanghai` 时区（UTC+8）
- 历史消息时间戳、通知中的时间都使用 UTC+8
- 前端显示时间也统一转换为 UTC+8

### 具体修改
- 日志初始化时指定时区为 `Asia/Shanghai`
- `datetime.now()` 改为 `datetime.now(pytz.utc).astimezone(shanghai_tz)`
- Webhook 通知时间格式：`2026-08-18 09:30:45 (UTC+8)`
- 数据库存储 UTC 时间戳，前端/后端展示转换为 UTC+8

---

## 六、部署方案（PM2 + Linux）

### 推荐架构（公网访问）

```
域名 → Nginx(HTTPS) → FastAPI(127.0.0.1:8765)
```

### PM2 配置示例
```ecosystem
{
  "apps": [
    {
      "name": "tg-monitor",
      "script": "python",
      "args": "web_app.py",
      "cwd": "/opt/tg-monitor",
      "interpreter": "/usr/bin/python3",
      "autorestart": true,
      "watch": false,
      "max_memory_restart": "256M",
      "log_date_format": "YYYY-MM-DD HH:mm:ss Z"
    }
  ]
}
```

### 启动命令
```bash
cd /opt/tg-monitor
pip install -r requirements.txt
pm2 start ecosystem.config.js
pm2 save
pm2 startup
```

### Nginx 反向代理配置
```nginx
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name your-domain.com;

    ssl_certificate /path/to/fullchain.pem;
    ssl_certificate_key /path/to/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:8765;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

### 安全配置
- 防火墙只开放 80/443 端口，`8765` 不对外暴露
- 使用 Let's Encrypt 免费 SSL 证书
- Cloudflare 可以套一层 CDN，隐藏真实 IP

---

## 七、工作量估算

| 模块 | 改动内容 | 涉及文件 | 工作量 |
|------|---------|---------|--------|
| 网站用户登录 | 登录页面、session 管理、鉴权中间件、登录限制 | web_app.py + templates/login.html + templates/index.html | 2 小时 |
| 企业微信机器人媒体推送 | 下载媒体 + 调用企业微信 API 上传推送 | web_app.py | 3 小时 |
| 历史消息记录 | SQLite 数据库存储、历史列表、筛选、Web 界面 | web_app.py + templates/index.html | 3 小时 |
| UTC+8 时间统一 | 修改 logger 和所有时间格式化 | web_app.py | 0.5 小时 |
| PM2 配置 | 添加 ecosystem.config.js 示例 | 新增文件 | 0.5 小时 |
| 更新 README | 新增部署说明 | README.md | 0.5 小时 |

**总计：约 9.5 小时**

---

## 八、最终方案确认

| 需求 | 确认方案 |
|------|---------|
| 用户登录 | ✅ 需要，无固定 IP，公网访问需要账号密码 |
| 通知方案 | ✅ 企业微信机器人，完整支持图片/视频/文件转发，国内微信查看不用 VPN |
| 媒体转发 | ✅ 图片、视频、贴纸、文件全部转发 |
| 历史消息 | ✅ 需要，Web 界面查看，支持按日期筛选 |
| 部署方式 | ✅ Linux + PM2 |
| 时区 | ✅ 所有时间统一为 UTC+8 |